# Data

`raw_data` holds the raw inputs (only the EA-MPD file is committed, the
rest is downloaded at run time). `data_for_analysis` holds the cleaned
data produced by `Programs/01_dataprep` and is not versioned.
