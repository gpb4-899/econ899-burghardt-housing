# Programs

All code. `01_dataprep` reads raw data and writes cleaned data,
`02_analysis` reads cleaned data and writes every exhibit to `Results`.
Run everything in order with `01_main.R` in the repository root.
